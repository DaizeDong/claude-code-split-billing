{
  "name": "claude-code-split-billing",
  "version": "2.1.198",
  "description": "Keep Claude Code's Remote Control (subscription/OAuth) while routing inference to a custom Anthropic-compatible gateway, so token usage is billed separately from your subscription quota — isolated, so plain `claude` is unaffected.",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "cc": "bin/cc"
  },
  "scripts": {
    "start": "bun src/proxy.js"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.1"
  },
  "files": [
    "src/",
    "bin/",
    "scripts/",
    "config/",
    "legacy/",
    ".env.example",
    "README.md",
    "LICENSE"
  ]
}
