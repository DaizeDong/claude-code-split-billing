# Secrets and environment
.env

# Certificate bundles are environment-specific and may be sensitive
*.pem
ca-bundle.pem

# Isolated Claude Code config holds OAuth credentials — never commit
.claude-config/

# Records which config dir cc uses (may be a personal absolute path)
.cc-config-dir

# Logs
*.log
proxy.log
proxy-stdout.log

# Node
node_modules/
